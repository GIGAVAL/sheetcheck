# The bid set PDF is large (43 MB) and downloadable from the source URL,
# so we don't commit it to git.
bidset.pdf
*.pdf

# Extraction cache (regenerated from the PDF on demand)
*.sheetcache.json

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/
